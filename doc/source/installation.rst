============
Installation
============

At the command line::

    $ pip install workloadmanager

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv workloadmanager
    $ pip install workloadmanager