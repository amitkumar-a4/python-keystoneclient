============
Installation
============

At the command line::

    $ pip install workloadmgr

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv workloadmgr
    $ pip install workloadmgr